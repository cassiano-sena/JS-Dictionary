let numero = 1;
{
    let numero = 2;
    console.log('dentro = ', numero);
};
console.log('fora = ', numero);

// A diferença entre var e let:

// Var é definido com escopo global e de função!

// Let é definido com escopo global, de função e de bloco!